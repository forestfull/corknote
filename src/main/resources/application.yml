jasypt:
  encryptor:
    password: ${spring.profiles.active}
salt: ${spring.profiles.active}
spring:
  application:
    name: corknote
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
tunneling:
  database:
    driver-class-name:
    url:
    username:
    password:
  ssh:
    user:
    host:
